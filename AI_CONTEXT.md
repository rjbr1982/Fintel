# Context for AI Session - Fintel App (דוחכם)

**Hello AI!** You are resuming work on the "Fintel" (דוחכם) project architecture.

### Where we left off:
1. Fixed the Unified Fund (קופה מאוחדת) UI bug in `CategoryDrilldownScreen`. Decoupled the display of the green unified frame and withdrawal buttons from the `isSinking` status of individual child expenses. The UI now relies strictly on the parent category's `isUnified` status from `BudgetProvider`, applying globally to all categories (e.g., Kids, Cars, Parents).
2. Codebase remains clean with zero Linter warnings (Zero Warnings Policy enforced).

### Next Steps:
Await explicit instructions from Rafael. Proceeding with the next items on the WhatsApp fix-list.

### Constitution Status:
Up to date (v12.20). סטטוס חוקה: תואמת לקוד (לא נדרשים שינויים בסשן זה).