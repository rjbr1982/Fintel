name: fintel
description: Financial Intelligence Application based on Dohaham method.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # State Management
  provider: ^6.1.1
  
  # Utilities
  intl: ^0.20.0 
  
  # Notifications
  flutter_local_notifications: ^17.0.0
  
  # System
  path_provider: ^2.1.5
  local_auth: ^2.2.0
  
  # SaaS & Firebase Cloud (Constitution 5.10)
  firebase_core: ^4.4.0 
  cloud_firestore: ^6.1.2 
  firebase_auth: ^6.1.4 
  google_sign_in: ^6.3.0 
  firebase_ui_auth: ^3.0.1 
  firebase_ui_oauth_google: ^2.0.1 
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  # מחולל אייקונים אוטומטי לכל הפלטפורמות
  flutter_launcher_icons: ^0.13.1

# הגדרות מחולל האייקונים (Web + Mobile)
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icon/Fintel_Icon.png"
  web:
    generate: true
    image_path: "assets/icon/Fintel_Icon.png"
    background_color: "#121212"
    theme_color: "#121212"

flutter:
  uses-material-design: true
  assets:
    - assets/icon/Fintel_Icon.png
    - assets/icon/splash.gif